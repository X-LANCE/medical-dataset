sql = Intersect(sql_unit, sql_unit)
        | Union(sql_unit, sql_unit)
        | Except(sql_unit, sql_unit)
        | Single(sql_unit)

sql_unit = Complete(select, from, cond, group_by, order_by)
        | NoWhere(select, from, group_by, order_by)
        | NoGroupBy(select, from, cond, order_by)
        | NoOrderBy(select, from, cond, group_by)
        | OnlyWhere(select, from, cond)
        | OnlyGroupBy(select, from, group_by)
        | OnlyOrderBy(select, from, order_by)
        | Simple(select, from)

select = SelectOne(agg_unit)
        | SelectTwo(agg_unit, agg_unit)
        | SelectThree(agg_unit, agg_unit, agg_unit)
        | SelectFour(agg_unit, agg_unit, agg_unit, agg_unit)
        | SelectFive(agg_unit, agg_unit, agg_unit, agg_unit, agg_unit)

from = FromOneTable(tab)
        | FromTwoTable(tab, tab)
        | FromThreeTable(tab, tab, tab)
        | FromSQL(sql)

group_by = OneNoHaving(col_unit)
        | TwoNoHaving(col_unit, col_unit)
        | OneHaving(col_unit, cond)
        | TwoHaving(col_unit, col_unit, cond)

order_by = OneAsc(agg_unit)
        | OneDesc(agg_unit)
        | OneAscLimit(agg_unit)
        | OneDescLimit(agg_unit)
        | TwoAsc(agg_unit, agg_unit)
        | TwoDesc(agg_unit, agg_unit)
        | TwoAscLimit(agg_unit, agg_unit)
        | TwoDescLimit(agg_unit, agg_unit)

cond = And(cond, cond)
        | Or(cond, cond)
        | Between(agg_unit)
        | Eq(agg_unit)
        | Gt(agg_unit)
        | Lt(agg_unit)
        | Ge(agg_unit)
        | Le(agg_unit)
        | Neq(agg_unit)
        | Like(agg_unit)
        | EqSQL(agg_unit, sql)
        | GtSQL(agg_unit, sql)
        | LtSQL(agg_unit, sql)
        | GeSQL(agg_unit, sql)
        | LeSQL(agg_unit, sql)
        | NeqSQL(agg_unit, sql)
        | InSQL(agg_unit, sql)
        | NotInSQL(agg_unit, sql)

agg_unit = None(val_unit)
        | Max(val_unit)
        | Min(val_unit)
        | Count(val_unit)
        | Sum(val_unit)
        | Avg(val_unit)

val_unit = Unary(col_unit)
        | Minus(col_unit, col_unit)
        | Plus(col_unit, col_unit)
        | Times(col_unit, col_unit)
        | Divide(col_unit, col_unit)

col_unit = Column(col)